/*
 * SwaggerPetstoreLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package io.swagger.petstore.controllers;

import io.swagger.petstore.Environment;
import io.swagger.petstore.SwaggerPetstoreClient;
import io.swagger.petstore.authentication.ApiKeyModel;
import io.swagger.petstore.authentication.HttpBasicModel;
import io.swagger.petstore.authentication.PetstoreAuthModel;
import io.swagger.petstore.models.OAuthToken;
import io.swagger.petstore.testing.HttpCallbackCatcher;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.rules.Timeout;

/**
 * Base class for all test cases.
 */
public class BaseControllerTest {
    /**
     * Test configuration.
     */
    public static final int REQUEST_TIMEOUT = 30;

    public static final double ASSERT_PRECISION = 0.01;
    
    /**
     * Global rules for tests.
     */
    @Rule
    public Timeout globalTimeout = Timeout.seconds(REQUEST_TIMEOUT);
    
    /**
     * Test fixtures,
     * Used to serve as HttpCallback and to capture request & response.
     */

    protected static HttpCallbackCatcher httpResponse;
    
    /**
     * Setup test.
     */
    @BeforeClass
    public static void setUp() throws Exception {
        httpResponse = new HttpCallbackCatcher(); 
    }

    /**
     * Tear down test.
     */
    @AfterClass
    public static void tearDown() throws Exception {
        httpResponse = null;
    }

    /**
     * Create test configuration from Environment variables.
     */
    protected static SwaggerPetstoreClient createConfigurationFromEnvironment() {
        SwaggerPetstoreClient.Builder builder = new SwaggerPetstoreClient.Builder();

        final String environment = System.getenv("SWAGGER_PETSTORE_LIB_ENVIRONMENT");
        final String testHeader = System.getenv("SWAGGER_PETSTORE_LIB_TEST_HEADER");
        final String timeout = System.getenv("SWAGGER_PETSTORE_LIB_TIMEOUT");
        final String apiKey = System.getenv("SWAGGER_PETSTORE_LIB_API_KEY");
        final String username = System.getenv("SWAGGER_PETSTORE_LIB_USERNAME");
        final String passwprd = System.getenv("SWAGGER_PETSTORE_LIB_PASSWPRD");
        final String oAuthClientId = System.getenv("SWAGGER_PETSTORE_LIB_O_AUTH_CLIENT_ID");
        final String oAuthRedirectUri = System.getenv("SWAGGER_PETSTORE_LIB_O_AUTH_REDIRECT_URI");

        if (apiKey != null) {
            builder.apiKeyCredentials(new ApiKeyModel
                    .Builder(apiKey)
                    .build());
        }
        if (username != null && passwprd != null) {
            builder.httpBasicCredentials(new HttpBasicModel
                    .Builder(username, passwprd)
                    .build());
        }
        if (oAuthClientId != null && oAuthRedirectUri != null) {
            builder.petstoreAuthCredentials(new PetstoreAuthModel
                    .Builder(oAuthClientId, oAuthRedirectUri)
                    .build());
        }
        if (environment != null) {
            builder.environment(Environment.fromString(environment));
        }
        if (testHeader != null) {
            builder.testHeader(testHeader);
        }
        if (timeout != null) {
            builder.httpClientConfig(configBuilder -> configBuilder.timeout(
                    Long.parseLong(timeout)));
        }
        return builder.build();
    }

    /**
     * Create test configuration.
     */
    protected static SwaggerPetstoreClient createConfiguration() {
        // Set Configuration parameters for test execution
        SwaggerPetstoreClient config = createConfigurationFromEnvironment();
        config = config.newBuilder()
                .httpBasicCredentials(new HttpBasicModel
                        .Builder("test", "testPassword").build())
                .httpCallback(httpResponse)
                .build();
        return config;
    }
}
