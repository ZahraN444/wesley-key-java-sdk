/*
 * QuickStartAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.transtream.apimatic.controllers;

import com.transtream.apimatic.ApiHelper;
import com.transtream.apimatic.Server;
import com.transtream.apimatic.exceptions.ApiException;
import com.transtream.apimatic.http.request.HttpMethod;
import com.transtream.apimatic.models.PierbridgeAddressValidationRequest;
import com.transtream.apimatic.models.PierbridgeAddressValidationResponse;
import com.transtream.apimatic.models.PierbridgeRateRequest;
import com.transtream.apimatic.models.PierbridgeRateResponse;
import com.transtream.apimatic.models.PierbridgeRateShopRequest;
import com.transtream.apimatic.models.PierbridgeRateShopResponse;
import com.transtream.apimatic.models.PierbridgeShipRequest;
import com.transtream.apimatic.models.PierbridgeShipResponse;
import com.transtream.apimatic.models.PierbridgeTrackRequest;
import com.transtream.apimatic.models.PierbridgeTrackResponse;
import com.transtream.apimatic.models.PierbridgeVoidRequest;
import com.transtream.apimatic.models.PierbridgeVoidResponse;
import io.apimatic.core.ApiCall;
import io.apimatic.core.GlobalConfiguration;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;
import javax.xml.bind.JAXBException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class QuickStartAPIController extends BaseController {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public QuickStartAPIController(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Address validation checks an address against a carrier's records. When used within the UI,
     * typically in a shipping app under the Ship To tab, the VALIDATE button will return a VALID
     * response if the check is good, or a popout box showing an updated address from the carrier
     * which can then be accepted if desired. The carrier in use by default for address validation
     * can be assigned via the Administration App, under Carriers. On our test system, our default
     * address validation carrier is set to FedEx Web Services (ID 49), as they have a robust
     * database of addresses to check against. However, you can additionally test with another
     * carrier by specifying the Carrier in the parameters below. Using Carrier ID 7, for instance,
     * will use DHL XML-PI to perform the validation. For more information on supported carriers and
     * their IDs, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/CarriersList.htm"&gt;All Carriers
     * Matrix&lt;/a&gt; reference. Additionally, more information regarding the Address Validation can be
     * found here: &lt;a
     * href="http://documentation.transtream.com/Content/AddressValidation/Introduction.htm"&gt;Address
     * Validation&lt;/a&gt;.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeAddressValidationResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeAddressValidationResponse addressValidation(
            final PierbridgeAddressValidationRequest body) throws ApiException, IOException, JAXBException {
        return prepareAddressValidationRequest(body).execute();
    }

    /**
     * Address validation checks an address against a carrier's records. When used within the UI,
     * typically in a shipping app under the Ship To tab, the VALIDATE button will return a VALID
     * response if the check is good, or a popout box showing an updated address from the carrier
     * which can then be accepted if desired. The carrier in use by default for address validation
     * can be assigned via the Administration App, under Carriers. On our test system, our default
     * address validation carrier is set to FedEx Web Services (ID 49), as they have a robust
     * database of addresses to check against. However, you can additionally test with another
     * carrier by specifying the Carrier in the parameters below. Using Carrier ID 7, for instance,
     * will use DHL XML-PI to perform the validation. For more information on supported carriers and
     * their IDs, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/CarriersList.htm"&gt;All Carriers
     * Matrix&lt;/a&gt; reference. Additionally, more information regarding the Address Validation can be
     * found here: &lt;a
     * href="http://documentation.transtream.com/Content/AddressValidation/Introduction.htm"&gt;Address
     * Validation&lt;/a&gt;.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeAddressValidationResponse response from the API call
     */
    public CompletableFuture<PierbridgeAddressValidationResponse> addressValidationAsync(
            final PierbridgeAddressValidationRequest body) {
        try {
            return prepareAddressValidationRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for addressValidation.
     */
    private ApiCall<PierbridgeAddressValidationResponse, ApiException> prepareAddressValidationRequest(
            final PierbridgeAddressValidationRequest body) {
        return new ApiCall.Builder<PierbridgeAddressValidationResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeAddressValidationRequest", PierbridgeAddressValidationRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeAddressValidationResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Running a rate request will send certain shipment parameters to the carrier to return
     * expected charges. In many cases, this can be as simple as providing the receiver's address
     * and some package details. When some details such as the sender's address is not specified,
     * the system uses the shipping user's assigned location to determine the origin address. Other
     * details can be omitted and will be included from the defaults already configured in the
     * system defaults, allowing requests to be simple when default values will be sufficient.
     * Additional details can be provided, however, by filling in further parameters below. Some
     * details that may be needed can include line item information for products contained in the
     * package, content descriptions, or declared values. Additionally, special services can be
     * included as well. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This rate request is using Carrier 3 for UPS API and ServiceType 25
     * for UPS Ground.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeRateResponse rateRequest(
            final PierbridgeRateRequest body) throws ApiException, IOException, JAXBException {
        return prepareRateRequestRequest(body).execute();
    }

    /**
     * Running a rate request will send certain shipment parameters to the carrier to return
     * expected charges. In many cases, this can be as simple as providing the receiver's address
     * and some package details. When some details such as the sender's address is not specified,
     * the system uses the shipping user's assigned location to determine the origin address. Other
     * details can be omitted and will be included from the defaults already configured in the
     * system defaults, allowing requests to be simple when default values will be sufficient.
     * Additional details can be provided, however, by filling in further parameters below. Some
     * details that may be needed can include line item information for products contained in the
     * package, content descriptions, or declared values. Additionally, special services can be
     * included as well. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This rate request is using Carrier 3 for UPS API and ServiceType 25
     * for UPS Ground.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateResponse response from the API call
     */
    public CompletableFuture<PierbridgeRateResponse> rateRequestAsync(
            final PierbridgeRateRequest body) {
        try {
            return prepareRateRequestRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for rateRequest.
     */
    private ApiCall<PierbridgeRateResponse, ApiException> prepareRateRequestRequest(
            final PierbridgeRateRequest body) {
        return new ApiCall.Builder<PierbridgeRateResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeRateRequest", PierbridgeRateRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeRateResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * The track request uses a PackageID to query the package history and return a status. For
     * testing purposes, a sample PackageID has been included here to test, however, as the shipment
     * was performed as a test shipment, no valid tracking information is returned. If you have
     * performed a test shipment using the Ship Request endpoint, the response provided will contain
     * a PackageID for that shipment that can be substituted here. Again, it will not return valid
     * tracking info as it is a test shipment, but you can see how to simply process a shipment and
     * then track it with the returned data.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeTrackResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeTrackResponse trackRequest(
            final PierbridgeTrackRequest body) throws ApiException, IOException, JAXBException {
        return prepareTrackRequestRequest(body).execute();
    }

    /**
     * The track request uses a PackageID to query the package history and return a status. For
     * testing purposes, a sample PackageID has been included here to test, however, as the shipment
     * was performed as a test shipment, no valid tracking information is returned. If you have
     * performed a test shipment using the Ship Request endpoint, the response provided will contain
     * a PackageID for that shipment that can be substituted here. Again, it will not return valid
     * tracking info as it is a test shipment, but you can see how to simply process a shipment and
     * then track it with the returned data.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeTrackResponse response from the API call
     */
    public CompletableFuture<PierbridgeTrackResponse> trackRequestAsync(
            final PierbridgeTrackRequest body) {
        try {
            return prepareTrackRequestRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for trackRequest.
     */
    private ApiCall<PierbridgeTrackResponse, ApiException> prepareTrackRequestRequest(
            final PierbridgeTrackRequest body) {
        return new ApiCall.Builder<PierbridgeTrackResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeTrackRequest", PierbridgeTrackRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeTrackResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * A rate shop uses a pre-defined group of carriers and services to return rates for comparison.
     * These rates can allow the user to review for a supported delivery date, a supported special
     * service, or simply the best shipping price. Rate groups are configured in the Administration
     * App under Carriers, and each has a specific ID associated with it for the purposes of
     * performing a rate shop. In our demo code here, we are using Rate Group 9, which is configured
     * in the system to rate shop using several carriers for domestic Ground shipments. We have
     * selected FedEx Web Services Home Delivery for residential, and FedEx Web Services Ground, UPS
     * API Ground, and USPS Postage First-Class Mail as services to rate for non-residential. When
     * the rate shop is performed, all valid rates will be returned for the shipment parameters
     * provided. As such, not every carrier/service will always return a valid rate when performing
     * a rate shop. For example, as FedEx Ground and Home Delivery are meant to be used exclusively
     * between residential or non-residential addresses, only one will return a valid rate if both
     * selected in the rate group, based on the destination address's residential flag. They have
     * been included for comparison purposes but may error if the address is not valid for the
     * service. Additionally, other parameters may come into play when obtaining a rate, such as
     * weight limits or minimums, or supported origin/destination areas. Check with your carrier to
     * verify what they accept and review the returned messages to see if any parameters are not
     * supported.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateShopResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeRateShopResponse rateShop(
            final PierbridgeRateShopRequest body) throws ApiException, IOException, JAXBException {
        return prepareRateShopRequest(body).execute();
    }

    /**
     * A rate shop uses a pre-defined group of carriers and services to return rates for comparison.
     * These rates can allow the user to review for a supported delivery date, a supported special
     * service, or simply the best shipping price. Rate groups are configured in the Administration
     * App under Carriers, and each has a specific ID associated with it for the purposes of
     * performing a rate shop. In our demo code here, we are using Rate Group 9, which is configured
     * in the system to rate shop using several carriers for domestic Ground shipments. We have
     * selected FedEx Web Services Home Delivery for residential, and FedEx Web Services Ground, UPS
     * API Ground, and USPS Postage First-Class Mail as services to rate for non-residential. When
     * the rate shop is performed, all valid rates will be returned for the shipment parameters
     * provided. As such, not every carrier/service will always return a valid rate when performing
     * a rate shop. For example, as FedEx Ground and Home Delivery are meant to be used exclusively
     * between residential or non-residential addresses, only one will return a valid rate if both
     * selected in the rate group, based on the destination address's residential flag. They have
     * been included for comparison purposes but may error if the address is not valid for the
     * service. Additionally, other parameters may come into play when obtaining a rate, such as
     * weight limits or minimums, or supported origin/destination areas. Check with your carrier to
     * verify what they accept and review the returned messages to see if any parameters are not
     * supported.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateShopResponse response from the API call
     */
    public CompletableFuture<PierbridgeRateShopResponse> rateShopAsync(
            final PierbridgeRateShopRequest body) {
        try {
            return prepareRateShopRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for rateShop.
     */
    private ApiCall<PierbridgeRateShopResponse, ApiException> prepareRateShopRequest(
            final PierbridgeRateShopRequest body) {
        return new ApiCall.Builder<PierbridgeRateShopResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeRateShopRequest", PierbridgeRateShopRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeRateShopResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * The void request uses a PackageID to void a previously produced shipment/label. For testing
     * purposes, a sample PackageID has been included here to test, however, as the shipment was
     * performed as a test shipment, the void request will respond that the record is not found. If
     * you have performed a test shipment using the Ship Request endpoint, the response provided
     * will contain a PackageID for that shipment that can be substituted here. Using this ID in the
     * void request will void the test shipment you previously produced.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeVoidResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeVoidResponse voidRequest(
            final PierbridgeVoidRequest body) throws ApiException, IOException, JAXBException {
        return prepareVoidRequestRequest(body).execute();
    }

    /**
     * The void request uses a PackageID to void a previously produced shipment/label. For testing
     * purposes, a sample PackageID has been included here to test, however, as the shipment was
     * performed as a test shipment, the void request will respond that the record is not found. If
     * you have performed a test shipment using the Ship Request endpoint, the response provided
     * will contain a PackageID for that shipment that can be substituted here. Using this ID in the
     * void request will void the test shipment you previously produced.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeVoidResponse response from the API call
     */
    public CompletableFuture<PierbridgeVoidResponse> voidRequestAsync(
            final PierbridgeVoidRequest body) {
        try {
            return prepareVoidRequestRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for voidRequest.
     */
    private ApiCall<PierbridgeVoidResponse, ApiException> prepareVoidRequestRequest(
            final PierbridgeVoidRequest body) {
        return new ApiCall.Builder<PierbridgeVoidResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeVoidRequest", PierbridgeVoidRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeVoidResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * The ship request is where the bulk of our processes are performed. This is where you provide
     * the full shipment details to send to the carrier. Some shipments, such as domestic US ground
     * shipments, require very little information to generate a label. Just as in the rate request,
     * the system uses defaults already configured to fill in details when not provided. Further
     * details can be provided, however, by filling in further parameters below. Some details that
     * may be needed can include line item information for products contained in the package,
     * content descriptions, or declared values. Additionally, special services can be included as
     * well. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This ship request is using Carrier 3 for UPS API and ServiceType 25
     * for UPS Ground.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeShipResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeShipResponse shipRequest(
            final PierbridgeShipRequest body) throws ApiException, IOException, JAXBException {
        return prepareShipRequestRequest(body).execute();
    }

    /**
     * The ship request is where the bulk of our processes are performed. This is where you provide
     * the full shipment details to send to the carrier. Some shipments, such as domestic US ground
     * shipments, require very little information to generate a label. Just as in the rate request,
     * the system uses defaults already configured to fill in details when not provided. Further
     * details can be provided, however, by filling in further parameters below. Some details that
     * may be needed can include line item information for products contained in the package,
     * content descriptions, or declared values. Additionally, special services can be included as
     * well. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This ship request is using Carrier 3 for UPS API and ServiceType 25
     * for UPS Ground.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeShipResponse response from the API call
     */
    public CompletableFuture<PierbridgeShipResponse> shipRequestAsync(
            final PierbridgeShipRequest body) {
        try {
            return prepareShipRequestRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for shipRequest.
     */
    private ApiCall<PierbridgeShipResponse, ApiException> prepareShipRequestRequest(
            final PierbridgeShipRequest body) {
        return new ApiCall.Builder<PierbridgeShipResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeShipRequest", PierbridgeShipRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeShipResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint is the same as the Rate Request, but has been preset to rate a simple
     * international shipment. The parameters needed are generally the same as for domestic rating,
     * but for some carriers can require additional line item or customs detail. For more
     * information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This rate request is using Carrier 3 for UPS API and ServiceType 26
     * for UPS Worldwide Express.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeRateResponse rateRequestInternational(
            final PierbridgeRateRequest body) throws ApiException, IOException, JAXBException {
        return prepareRateRequestInternationalRequest(body).execute();
    }

    /**
     * This endpoint is the same as the Rate Request, but has been preset to rate a simple
     * international shipment. The parameters needed are generally the same as for domestic rating,
     * but for some carriers can require additional line item or customs detail. For more
     * information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This rate request is using Carrier 3 for UPS API and ServiceType 26
     * for UPS Worldwide Express.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeRateResponse response from the API call
     */
    public CompletableFuture<PierbridgeRateResponse> rateRequestInternationalAsync(
            final PierbridgeRateRequest body) {
        try {
            return prepareRateRequestInternationalRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for rateRequestInternational.
     */
    private ApiCall<PierbridgeRateResponse, ApiException> prepareRateRequestInternationalRequest(
            final PierbridgeRateRequest body) {
        return new ApiCall.Builder<PierbridgeRateResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeRateRequest", PierbridgeRateRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeRateResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This endpoint is the same as the Ship Request, but has been preset to ship a simple
     * international shipment. The parameters here have been increased to include the International
     * and Content nodes that contain line item detail required for international shipments from
     * most carriers. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This ship request is using Carrier 3 for UPS API and ServiceType 26
     * for UPS Worldwide Express.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeShipResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public PierbridgeShipResponse shipRequestInternational(
            final PierbridgeShipRequest body) throws ApiException, IOException, JAXBException {
        return prepareShipRequestInternationalRequest(body).execute();
    }

    /**
     * This endpoint is the same as the Ship Request, but has been preset to ship a simple
     * international shipment. The parameters here have been increased to include the International
     * and Content nodes that contain line item detail required for international shipments from
     * most carriers. For more information regarding carrier services, please see the &lt;a
     * href="http://documentation.transtream.com/Content/Carriers/SupportedCarriers.htm"&gt;Supported
     * Carriers&lt;/a&gt; reference. This ship request is using Carrier 3 for UPS API and ServiceType 26
     * for UPS Worldwide Express.
     * @param  body  Required parameter: Example:
     * @return    Returns the PierbridgeShipResponse response from the API call
     */
    public CompletableFuture<PierbridgeShipResponse> shipRequestInternationalAsync(
            final PierbridgeShipRequest body) {
        try {
            return prepareShipRequestInternationalRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for shipRequestInternational.
     */
    private ApiCall<PierbridgeShipResponse, ApiException> prepareShipRequestInternationalRequest(
            final PierbridgeShipRequest body) {
        return new ApiCall.Builder<PierbridgeShipResponse, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/xmlservice.aspx")
                        .bodyParam(param -> param.value(body))
                        .bodySerializer(()  -> ApiHelper.serializeXml(body, "PierbridgeShipRequest", PierbridgeShipRequest.class))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .deserializer(
                                response -> ApiHelper.deserializeXml(response,
                                        PierbridgeShipResponse.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}