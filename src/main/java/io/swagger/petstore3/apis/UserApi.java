/*
 * SwaggerPetstoreOpenApi30Lib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package io.swagger.petstore3.apis;

import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import io.swagger.petstore3.ApiHelper;
import io.swagger.petstore3.Server;
import io.swagger.petstore3.exceptions.ApiException;
import io.swagger.petstore3.http.request.HttpMethod;
import io.swagger.petstore3.http.response.ApiResponse;
import io.swagger.petstore3.models.User;
import java.io.IOException;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;
import javax.xml.bind.JAXBException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class UserApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public UserApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * This can only be done by the logged in user.
     * @param  id  Optional parameter:
     * @param  username  Optional parameter:
     * @param  firstName  Optional parameter:
     * @param  lastName  Optional parameter:
     * @param  email  Optional parameter:
     * @param  password  Optional parameter:
     * @param  phone  Optional parameter:
     * @param  userStatus  Optional parameter: User Status
     * @return    Returns the User wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<User> createUser(
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) throws ApiException, IOException {
        return prepareCreateUserRequest(id, username, firstName, lastName, email, password, phone,
                userStatus).execute();
    }

    /**
     * This can only be done by the logged in user.
     * @param  id  Optional parameter:
     * @param  username  Optional parameter:
     * @param  firstName  Optional parameter:
     * @param  lastName  Optional parameter:
     * @param  email  Optional parameter:
     * @param  password  Optional parameter:
     * @param  phone  Optional parameter:
     * @param  userStatus  Optional parameter: User Status
     * @return    Returns the User wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<User>> createUserAsync(
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) {
        try {
            return prepareCreateUserRequest(id, username, firstName, lastName, email, password, phone,
            userStatus).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for createUser.
     */
    private ApiCall<ApiResponse<User>, ApiException> prepareCreateUserRequest(
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) {
        return new ApiCall.Builder<ApiResponse<User>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user")
                        .formParam(param -> param.key("id")
                                .value(id).isRequired(false))
                        .formParam(param -> param.key("username")
                                .value(username).isRequired(false))
                        .formParam(param -> param.key("firstName")
                                .value(firstName).isRequired(false))
                        .formParam(param -> param.key("lastName")
                                .value(lastName).isRequired(false))
                        .formParam(param -> param.key("email")
                                .value(email).isRequired(false))
                        .formParam(param -> param.key("password")
                                .value(password).isRequired(false))
                        .formParam(param -> param.key("phone")
                                .value(phone).isRequired(false))
                        .formParam(param -> param.key("userStatus")
                                .value(userStatus).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, User.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Creates list of users with given input array.
     * @param  body  Optional parameter:
     * @return    Returns the User wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<User> createUsersWithListInput(
            final List<User> body) throws ApiException, IOException {
        return prepareCreateUsersWithListInputRequest(body).execute();
    }

    /**
     * Creates list of users with given input array.
     * @param  body  Optional parameter:
     * @return    Returns the User wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<User>> createUsersWithListInputAsync(
            final List<User> body) {
        try {
            return prepareCreateUsersWithListInputRequest(body).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for createUsersWithListInput.
     */
    private ApiCall<ApiResponse<User>, ApiException> prepareCreateUsersWithListInputRequest(
            final List<User> body) {
        return new ApiCall.Builder<ApiResponse<User>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/createWithList")
                        .bodyParam(param -> param.value(body).isRequired(false))
                        .bodySerializer(() ->  ApiHelper.serialize(body))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/json").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, User.class))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Log into the system.
     * @param  username  Optional parameter: The user name for login
     * @param  password  Optional parameter: The password for login in clear text
     * @return    Returns the String wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     * @throws    JAXBException    Signals that a JAXB exception occurred.
     */
    public ApiResponse<String> loginUser(
            final String username,
            final String password) throws ApiException, IOException, JAXBException {
        return prepareLoginUserRequest(username, password).execute();
    }

    /**
     * Log into the system.
     * @param  username  Optional parameter: The user name for login
     * @param  password  Optional parameter: The password for login in clear text
     * @return    Returns the String wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<String>> loginUserAsync(
            final String username,
            final String password) {
        try {
            return prepareLoginUserRequest(username, password).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for loginUser.
     */
    private ApiCall<ApiResponse<String>, ApiException> prepareLoginUserRequest(
            final String username,
            final String password) {
        return new ApiCall.Builder<ApiResponse<String>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/login")
                        .queryParam(param -> param.key("username")
                                .value(username).isRequired(false))
                        .queryParam(param -> param.key("password")
                                .value(password).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/xml"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeXml(response, String.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid username/password supplied",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Log user out of the system.
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> logoutUser() throws ApiException, IOException {
        return prepareLogoutUserRequest().execute();
    }

    /**
     * Log user out of the system.
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> logoutUserAsync() {
        try {
            return prepareLogoutUserRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for logoutUser.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareLogoutUserRequest() {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/logout")
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get user detail based on username.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @return    Returns the User wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<User> getUserByName(
            final String usersname) throws ApiException, IOException {
        return prepareGetUserByNameRequest(usersname).execute();
    }

    /**
     * Get user detail based on username.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @return    Returns the User wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<User>> getUserByNameAsync(
            final String usersname) {
        try {
            return prepareGetUserByNameRequest(usersname).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getUserByName.
     */
    private ApiCall<ApiResponse<User>, ApiException> prepareGetUserByNameRequest(
            final String usersname) {
        return new ApiCall.Builder<ApiResponse<User>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/{usersname}")
                        .templateParam(param -> param.key("usersname").value(usersname)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, User.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid username supplied",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("User not found",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This can only be done by the logged in user.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @param  id  Optional parameter:
     * @param  username  Optional parameter:
     * @param  firstName  Optional parameter:
     * @param  lastName  Optional parameter:
     * @param  email  Optional parameter:
     * @param  password  Optional parameter:
     * @param  phone  Optional parameter:
     * @param  userStatus  Optional parameter: User Status
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> updateUser(
            final String usersname,
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) throws ApiException, IOException {
        return prepareUpdateUserRequest(usersname, id, username, firstName, lastName, email,
                password, phone, userStatus).execute();
    }

    /**
     * This can only be done by the logged in user.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @param  id  Optional parameter:
     * @param  username  Optional parameter:
     * @param  firstName  Optional parameter:
     * @param  lastName  Optional parameter:
     * @param  email  Optional parameter:
     * @param  password  Optional parameter:
     * @param  phone  Optional parameter:
     * @param  userStatus  Optional parameter: User Status
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> updateUserAsync(
            final String usersname,
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) {
        try {
            return prepareUpdateUserRequest(usersname, id, username, firstName, lastName, email, password,
            phone, userStatus).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for updateUser.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareUpdateUserRequest(
            final String usersname,
            final Long id,
            final String username,
            final String firstName,
            final String lastName,
            final String email,
            final String password,
            final String phone,
            final Integer userStatus) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/{usersname}")
                        .formParam(param -> param.key("id")
                                .value(id).isRequired(false))
                        .formParam(param -> param.key("username")
                                .value(username).isRequired(false))
                        .formParam(param -> param.key("firstName")
                                .value(firstName).isRequired(false))
                        .formParam(param -> param.key("lastName")
                                .value(lastName).isRequired(false))
                        .formParam(param -> param.key("email")
                                .value(email).isRequired(false))
                        .formParam(param -> param.key("password")
                                .value(password).isRequired(false))
                        .formParam(param -> param.key("phone")
                                .value(phone).isRequired(false))
                        .formParam(param -> param.key("userStatus")
                                .value(userStatus).isRequired(false))
                        .templateParam(param -> param.key("usersname").value(usersname)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .httpMethod(HttpMethod.PUT))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("bad request",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("user not found",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * This can only be done by the logged in user.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> deleteUser(
            final String usersname) throws ApiException, IOException {
        return prepareDeleteUserRequest(usersname).execute();
    }

    /**
     * This can only be done by the logged in user.
     * @param  usersname  Required parameter: The username that needs to be processed
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> deleteUserAsync(
            final String usersname) {
        try {
            return prepareDeleteUserRequest(usersname).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for deleteUser.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareDeleteUserRequest(
            final String usersname) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/user/{usersname}")
                        .templateParam(param -> param.key("usersname").value(usersname)
                                .shouldEncode(true))
                        .httpMethod(HttpMethod.DELETE))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid username supplied",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("User not found",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}