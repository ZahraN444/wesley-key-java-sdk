/*
 * SwaggerPetstoreOpenApi30Lib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package io.swagger.petstore3.apis;

import com.fasterxml.jackson.core.type.TypeReference;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import io.swagger.petstore3.ApiHelper;
import io.swagger.petstore3.DateTimeHelper;
import io.swagger.petstore3.Server;
import io.swagger.petstore3.exceptions.ApiException;
import io.swagger.petstore3.http.request.HttpMethod;
import io.swagger.petstore3.http.response.ApiResponse;
import io.swagger.petstore3.models.Order;
import io.swagger.petstore3.models.OrderStatus;
import java.io.IOException;
import java.time.LocalDateTime;
import java.util.Map;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class StoreApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public StoreApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Returns a map of status codes to quantities.
     * @return    Returns the Map of String, Integer wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Map<String, Integer>> getInventory() throws ApiException, IOException {
        return prepareGetInventoryRequest().execute();
    }

    /**
     * Returns a map of status codes to quantities.
     * @return    Returns the Map of String, Integer wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Map<String, Integer>>> getInventoryAsync() {
        try {
            return prepareGetInventoryRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getInventory.
     */
    private ApiCall<ApiResponse<Map<String, Integer>>, ApiException> prepareGetInventoryRequest() {
        return new ApiCall.Builder<ApiResponse<Map<String, Integer>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/store/inventory")
                        .withAuth(auth -> auth
                                .add("api_key"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response,
                                        new TypeReference<Map<String, Integer>>(){}))
                        .nullify404(false)
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Place a new order in the store.
     * @param  id  Optional parameter:
     * @param  petId  Optional parameter:
     * @param  quantity  Optional parameter:
     * @param  shipDate  Optional parameter:
     * @param  status  Optional parameter: Order Status
     * @param  complete  Optional parameter:
     * @return    Returns the Order wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Order> placeOrder(
            final Long id,
            final Long petId,
            final Integer quantity,
            final LocalDateTime shipDate,
            final OrderStatus status,
            final Boolean complete) throws ApiException, IOException {
        return preparePlaceOrderRequest(id, petId, quantity, shipDate, status, complete).execute();
    }

    /**
     * Place a new order in the store.
     * @param  id  Optional parameter:
     * @param  petId  Optional parameter:
     * @param  quantity  Optional parameter:
     * @param  shipDate  Optional parameter:
     * @param  status  Optional parameter: Order Status
     * @param  complete  Optional parameter:
     * @return    Returns the Order wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Order>> placeOrderAsync(
            final Long id,
            final Long petId,
            final Integer quantity,
            final LocalDateTime shipDate,
            final OrderStatus status,
            final Boolean complete) {
        try {
            return preparePlaceOrderRequest(id, petId, quantity, shipDate, status, complete).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for placeOrder.
     */
    private ApiCall<ApiResponse<Order>, ApiException> preparePlaceOrderRequest(
            final Long id,
            final Long petId,
            final Integer quantity,
            final LocalDateTime shipDate,
            final OrderStatus status,
            final Boolean complete) {
        return new ApiCall.Builder<ApiResponse<Order>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/store/order")
                        .formParam(param -> param.key("id")
                                .value(id).isRequired(false))
                        .formParam(param -> param.key("petId")
                                .value(petId).isRequired(false))
                        .formParam(param -> param.key("quantity")
                                .value(quantity).isRequired(false))
                        .formParam(param -> param.key("shipDate")
                                .value(DateTimeHelper.toRfc8601DateTime(shipDate)).isRequired(false))
                        .formParam(param -> param.key("status")
                                .value((status != null) ? status.value() : null).isRequired(false))
                        .formParam(param -> param.key("complete")
                                .value(complete).isRequired(false))
                        .headerParam(param -> param.key("Content-Type")
                                .value("application/x-www-form-urlencoded").isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.POST))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Order.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid input",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("422",
                                 ErrorCase.setReason("Validation exception",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * For valid response try integer IDs with value &lt;= 5 or &gt; 10. Other values will generate
     * exceptions.
     * @param  orderId  Required parameter: ID of order that needs to be fetched
     * @return    Returns the Order wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Order> getOrderById(
            final long orderId) throws ApiException, IOException {
        return prepareGetOrderByIdRequest(orderId).execute();
    }

    /**
     * For valid response try integer IDs with value &lt;= 5 or &gt; 10. Other values will generate
     * exceptions.
     * @param  orderId  Required parameter: ID of order that needs to be fetched
     * @return    Returns the Order wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Order>> getOrderByIdAsync(
            final long orderId) {
        try {
            return prepareGetOrderByIdRequest(orderId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getOrderById.
     */
    private ApiCall<ApiResponse<Order>, ApiException> prepareGetOrderByIdRequest(
            final long orderId) {
        return new ApiCall.Builder<ApiResponse<Order>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/store/order/{orderId}")
                        .templateParam(param -> param.key("orderId").value(orderId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Order.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid ID supplied",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Order not found",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * For valid response try integer IDs with value &lt; 1000. Anything above 1000 or non-integers
     * will generate API errors.
     * @param  orderId  Required parameter: ID of the order that needs to be deleted
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Void> deleteOrder(
            final long orderId) throws ApiException, IOException {
        return prepareDeleteOrderRequest(orderId).execute();
    }

    /**
     * For valid response try integer IDs with value &lt; 1000. Anything above 1000 or non-integers
     * will generate API errors.
     * @param  orderId  Required parameter: ID of the order that needs to be deleted
     * @return    Returns the Void wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Void>> deleteOrderAsync(
            final long orderId) {
        try {
            return prepareDeleteOrderRequest(orderId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for deleteOrder.
     */
    private ApiCall<ApiResponse<Void>, ApiException> prepareDeleteOrderRequest(
            final long orderId) {
        return new ApiCall.Builder<ApiResponse<Void>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/store/order/{orderId}")
                        .templateParam(param -> param.key("orderId").value(orderId).isRequired(false)
                                .shouldEncode(true))
                        .httpMethod(HttpMethod.DELETE))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("Invalid ID supplied",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("Order not found",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase(ErrorCase.DEFAULT,
                                 ErrorCase.setReason("Unexpected error",
                                (reason, context) -> new ApiException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}