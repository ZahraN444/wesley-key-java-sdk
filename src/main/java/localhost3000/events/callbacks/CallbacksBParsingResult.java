/*
 * WebhooksAndCallbacksAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package localhost3000.events.callbacks;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.apimatic.core.annotations.TypeCombinator.TypeCombinatorCase;
import java.io.IOException;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Map;
import localhost3000.ApiHelper;
import localhost3000.events.SignatureVerificationResult;
import localhost3000.models.NotificationCallback;

/**
 * This is an event class for CallbacksBParsingResult.
 */
@JsonDeserialize(using = CallbacksBParsingResult.CallbacksBParsingResultDeserializer.class)
public abstract class CallbacksBParsingResult {
    
    /**
     * Called when email notification delivery is complete
     * @param emailNotificationCallback NotificationCallback value for emailNotificationCallback.
     * @return The EmailNotificationCallbackCase object.
     */
    public static CallbacksBParsingResult fromEmailNotificationCallback(
            NotificationCallback emailNotificationCallback) {
        return emailNotificationCallback == null ? null : new EmailNotificationCallbackCase(emailNotificationCallback);
    }

    /**
     * Called when SMS notification delivery is complete
     * @param smsNotificationCallback NotificationCallback value for smsNotificationCallback.
     * @return The SmsNotificationCallbackCase object.
     */
    public static CallbacksBParsingResult fromSmsNotificationCallback(
            NotificationCallback smsNotificationCallback) {
        return smsNotificationCallback == null ? null : new SmsNotificationCallbackCase(smsNotificationCallback);
    }

    /**
     * Triggered when no event identified
     * @return The Unknown object.
     */
    public static CallbacksBParsingResult markUnknown() {
        return new Unknown();
    }

    /**
     * Triggered when signature verification failed
     * @param signatureVerificationResult SignatureVerificationResult value for signatureVerificationResult.
     * @return The SignatureVerificationFailed object.
     */
    public static CallbacksBParsingResult fromSignatureVerificationFailed(
            SignatureVerificationResult signatureVerificationResult) {
        return new SignatureVerificationFailed(signatureVerificationResult);
    }

    /**
     * Method to match from the provided one-of cases.
     * @param <R> The type to return after applying callback.
     * @param cases The one-of type cases callback.
     * @return The one-of matched case.
     */
    public abstract <R> R match(Cases<R> cases);

    /**
     * Method to match from the provided some-of cases (any one).
     * @param <R> The type to return after applying callback.
     * @param someCases The some-of type cases callback.
     * @return The matched case, or null if not handled.
     */
    public <R> R matchSome(SomeCases<R> someCases) {
        return match(new Cases<R>() {

            @Override
            public R emailNotificationCallback(NotificationCallback emailNotificationCallback) {
                return someCases.emailNotificationCallback(emailNotificationCallback);
            }

            @Override
            public R smsNotificationCallback(NotificationCallback smsNotificationCallback) {
                return someCases.smsNotificationCallback(smsNotificationCallback);
            }

            @Override
            public R unknown() {
                return someCases.unknown();
            }

            @Override
            public R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) {
                return someCases.signatureVerificationFailed(signatureVerificationResult);
            }
        });
    }

    /**
     * This is interface for CallbacksBParsingResult cases.
     * @param <R> The type to return after applying callback.
     */
    public interface Cases<R> {
        R emailNotificationCallback(NotificationCallback emailNotificationCallback);

        R smsNotificationCallback(NotificationCallback smsNotificationCallback);

        R unknown();

        R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult);
    }

    /**
     * This is interface for some-of cases (any subset).
     * @param <R> The type to return after applying callback.
     */
    public interface SomeCases<R> {
        default R emailNotificationCallback(NotificationCallback emailNotificationCallback) { return null; }

        default R smsNotificationCallback(NotificationCallback smsNotificationCallback) { return null; }

        default R unknown() { return null; }

        default R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) { return null; }
    }

    /**
     * This is a implementation class for EmailNotificationCallbackCase.
     */
    @JsonDeserialize(using = JsonDeserializer.None.class)
    @TypeCombinatorCase(type = "NotificationCallback")
    private static class EmailNotificationCallbackCase extends CallbacksBParsingResult {

        @JsonValue
        private NotificationCallback emailNotificationCallback;

        EmailNotificationCallbackCase(NotificationCallback emailNotificationCallback) {
            this.emailNotificationCallback = emailNotificationCallback;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.emailNotificationCallback(this.emailNotificationCallback);
        }

        @JsonCreator
        private EmailNotificationCallbackCase(JsonNode jsonNode) throws IOException {
            this.emailNotificationCallback = ApiHelper.deserialize(jsonNode,
                NotificationCallback.class);
        }

        @Override
        public String toString() {
            return emailNotificationCallback.toString();
        }
    }

    /**
     * This is a implementation class for SmsNotificationCallbackCase.
     */
    @JsonDeserialize(using = JsonDeserializer.None.class)
    @TypeCombinatorCase(type = "NotificationCallback")
    private static class SmsNotificationCallbackCase extends CallbacksBParsingResult {

        @JsonValue
        private NotificationCallback smsNotificationCallback;

        SmsNotificationCallbackCase(NotificationCallback smsNotificationCallback) {
            this.smsNotificationCallback = smsNotificationCallback;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.smsNotificationCallback(this.smsNotificationCallback);
        }

        @JsonCreator
        private SmsNotificationCallbackCase(JsonNode jsonNode) throws IOException {
            this.smsNotificationCallback = ApiHelper.deserialize(jsonNode,
                NotificationCallback.class);
        }

        @Override
        public String toString() {
            return smsNotificationCallback.toString();
        }
    }

    /**
     * This is a implementation class for UnknownCase.
     */
    private static class Unknown extends CallbacksBParsingResult {

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.unknown();
        }

    }

    /**
     * This is a implementation class for SignatureVerificationFailedCase.
     */
    private static class SignatureVerificationFailed extends CallbacksBParsingResult {

        private SignatureVerificationResult signatureVerificationResult;

        SignatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) {
            this.signatureVerificationResult = signatureVerificationResult;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.signatureVerificationFailed(this.signatureVerificationResult);
        }

    }

    /**
     * This is a custom deserializer class for CallbacksBParsingResult.
     */
    protected static class CallbacksBParsingResultDeserializer
            extends JsonDeserializer<CallbacksBParsingResult> {

        private String discriminator = "notificationType";
        private List<Map<String, Class<? extends CallbacksBParsingResult>>> registry =
                Arrays.asList(
                        Collections.singletonMap("email", EmailNotificationCallbackCase.class),
                        Collections.singletonMap("sms", SmsNotificationCallbackCase.class));

        @Override
        public CallbacksBParsingResult deserialize(JsonParser jp, DeserializationContext ctxt)
                throws IOException, JsonProcessingException {
            return ApiHelper.deserialize(jp, ctxt, discriminator, registry, null, true);
        }
    }

}
