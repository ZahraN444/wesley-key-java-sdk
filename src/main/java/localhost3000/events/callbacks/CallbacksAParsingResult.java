/*
 * WebhooksAndCallbacksAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package localhost3000.events.callbacks;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.ObjectCodec;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.apimatic.core.annotations.TypeCombinator.TypeCombinatorCase;
import java.io.IOException;
import java.util.Arrays;
import localhost3000.ApiHelper;
import localhost3000.events.SignatureVerificationResult;
import localhost3000.models.FulfillmentCallback;
import localhost3000.models.PaymentCallback;

/**
 * This is an event class for CallbacksAParsingResult.
 */
@JsonDeserialize(using = CallbacksAParsingResult.CallbacksAParsingResultDeserializer.class)
public abstract class CallbacksAParsingResult {
    
    /**
     * Called when payment processing is complete
     * @param paymentCallback PaymentCallback value for paymentCallback.
     * @return The PaymentCallbackCase object.
     */
    public static CallbacksAParsingResult fromPaymentCallback(PaymentCallback paymentCallback) {
        return paymentCallback == null ? null : new PaymentCallbackCase(paymentCallback);
    }

    /**
     * Called when order processing is complete
     * @param fulfillmentCallback FulfillmentCallback value for fulfillmentCallback.
     * @return The FulfillmentCallbackCase object.
     */
    public static CallbacksAParsingResult fromFulfillmentCallback(
            FulfillmentCallback fulfillmentCallback) {
        return fulfillmentCallback == null ? null : new FulfillmentCallbackCase(fulfillmentCallback);
    }

    /**
     * Triggered when no event identified
     * @return The Unknown object.
     */
    public static CallbacksAParsingResult markUnknown() {
        return new Unknown();
    }

    /**
     * Triggered when signature verification failed
     * @param signatureVerificationResult SignatureVerificationResult value for signatureVerificationResult.
     * @return The SignatureVerificationFailed object.
     */
    public static CallbacksAParsingResult fromSignatureVerificationFailed(
            SignatureVerificationResult signatureVerificationResult) {
        return new SignatureVerificationFailed(signatureVerificationResult);
    }

    /**
     * Method to match from the provided one-of cases.
     * @param <R> The type to return after applying callback.
     * @param cases The one-of type cases callback.
     * @return The one-of matched case.
     */
    public abstract <R> R match(Cases<R> cases);

    /**
     * Method to match from the provided some-of cases (any one).
     * @param <R> The type to return after applying callback.
     * @param someCases The some-of type cases callback.
     * @return The matched case, or null if not handled.
     */
    public <R> R matchSome(SomeCases<R> someCases) {
        return match(new Cases<R>() {

            @Override
            public R paymentCallback(PaymentCallback paymentCallback) {
                return someCases.paymentCallback(paymentCallback);
            }

            @Override
            public R fulfillmentCallback(FulfillmentCallback fulfillmentCallback) {
                return someCases.fulfillmentCallback(fulfillmentCallback);
            }

            @Override
            public R unknown() {
                return someCases.unknown();
            }

            @Override
            public R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) {
                return someCases.signatureVerificationFailed(signatureVerificationResult);
            }
        });
    }

    /**
     * This is interface for CallbacksAParsingResult cases.
     * @param <R> The type to return after applying callback.
     */
    public interface Cases<R> {
        R paymentCallback(PaymentCallback paymentCallback);

        R fulfillmentCallback(FulfillmentCallback fulfillmentCallback);

        R unknown();

        R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult);
    }

    /**
     * This is interface for some-of cases (any subset).
     * @param <R> The type to return after applying callback.
     */
    public interface SomeCases<R> {
        default R paymentCallback(PaymentCallback paymentCallback) { return null; }

        default R fulfillmentCallback(FulfillmentCallback fulfillmentCallback) { return null; }

        default R unknown() { return null; }

        default R signatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) { return null; }
    }

    /**
     * This is a implementation class for PaymentCallbackCase.
     */
    @JsonDeserialize(using = JsonDeserializer.None.class)
    @TypeCombinatorCase(type = "PaymentCallback")
    private static class PaymentCallbackCase extends CallbacksAParsingResult {

        @JsonValue
        private PaymentCallback paymentCallback;

        PaymentCallbackCase(PaymentCallback paymentCallback) {
            this.paymentCallback = paymentCallback;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.paymentCallback(this.paymentCallback);
        }

        @JsonCreator
        private PaymentCallbackCase(JsonNode jsonNode) throws IOException {
            this.paymentCallback = ApiHelper.deserialize(jsonNode,
                PaymentCallback.class);
        }

        @Override
        public String toString() {
            return paymentCallback.toString();
        }
    }

    /**
     * This is a implementation class for FulfillmentCallbackCase.
     */
    @JsonDeserialize(using = JsonDeserializer.None.class)
    @TypeCombinatorCase(type = "FulfillmentCallback")
    private static class FulfillmentCallbackCase extends CallbacksAParsingResult {

        @JsonValue
        private FulfillmentCallback fulfillmentCallback;

        FulfillmentCallbackCase(FulfillmentCallback fulfillmentCallback) {
            this.fulfillmentCallback = fulfillmentCallback;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.fulfillmentCallback(this.fulfillmentCallback);
        }

        @JsonCreator
        private FulfillmentCallbackCase(JsonNode jsonNode) throws IOException {
            this.fulfillmentCallback = ApiHelper.deserialize(jsonNode,
                FulfillmentCallback.class);
        }

        @Override
        public String toString() {
            return fulfillmentCallback.toString();
        }
    }

    /**
     * This is a implementation class for UnknownCase.
     */
    private static class Unknown extends CallbacksAParsingResult {

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.unknown();
        }

    }

    /**
     * This is a implementation class for SignatureVerificationFailedCase.
     */
    private static class SignatureVerificationFailed extends CallbacksAParsingResult {

        private SignatureVerificationResult signatureVerificationResult;

        SignatureVerificationFailed(SignatureVerificationResult signatureVerificationResult) {
            this.signatureVerificationResult = signatureVerificationResult;
        }

        @Override
        public <R> R match(Cases<R> cases) {
            return cases.signatureVerificationFailed(this.signatureVerificationResult);
        }

    }

    /**
     * This is a custom deserializer class for CallbacksAParsingResult.
     */
    protected static class CallbacksAParsingResultDeserializer
            extends JsonDeserializer<CallbacksAParsingResult> {

        @Override
        public CallbacksAParsingResult deserialize(JsonParser jp, DeserializationContext ctxt)
                throws IOException, JsonProcessingException {
            ObjectCodec oc = jp.getCodec();
            JsonNode node = oc.readTree(jp);
            return ApiHelper.deserialize(node, Arrays.asList(PaymentCallbackCase.class,
                    FulfillmentCallbackCase.class), true);
        }
    }

}
